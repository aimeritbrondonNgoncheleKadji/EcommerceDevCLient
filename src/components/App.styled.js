import styled from 'styled-components';

export default styled.div`
  max-width: 1400px;
  position: relative;
  display: flex;
  margin: 0 auto;

  > div {
    flex: 1;
    padding: 3rem 0;

    > header {
      margin-bottom: 2rem;
    }
  }
`;
